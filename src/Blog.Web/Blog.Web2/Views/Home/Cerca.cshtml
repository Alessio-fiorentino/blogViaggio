@model IEnumerable<Blog.Web2.Models.ViaggioRavendb>

@{
    ViewData["Title"] = "Cerca";
}

<h1>Cerca una esperienza di viaggio</h1>
<form asp-controller="Home" asp-action="Cerca" method="get">
    <p>
        Città: <input type="text" name="SearchString" />
        <input type="submit" value="Filter" />
    </p>
</form>

<table class="table">
    <thead>
        <tr>
            <th>
                Città
            </th>
            <th>
                Data
            </th>

            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Citta)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Periodo)
                </td>

                <td>
                    <a asp-action="Visualizza" asp-route-id="@item.Id">Details</a>

                </td>
            </tr>
        }
    </tbody>
</table>
